# Get your key at https://vybenetwork.com/pricing
# Use the same key as in Vybe docs/curl to get the same response.
VYBE_API_KEY=

# Optional: Solana mainnet RPC for token symbol lookup (Metaplex metadata). Default: https://api.mainnet-beta.solana.com
# SOLANA_RPC_URL=
